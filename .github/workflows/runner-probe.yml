name: Runner probe

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  probe:
    name: Confirm GitHub-hosted runner startup
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Confirm runner availability
        run: echo "GitHub Actions runner started successfully"
