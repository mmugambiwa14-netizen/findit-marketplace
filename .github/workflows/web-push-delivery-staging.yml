name: Web Push delivery — staging

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

permissions:
  contents: read

concurrency:
  group: web-push-delivery-staging
  cancel-in-progress: false

jobs:
  deliver:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: cloudflare-staging
    steps:
      - name: Validate required staging configuration
        env:
          SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL }}
          EXPECTED_PROJECT_REF: ${{ vars.FINDIT_EXPECTED_PROJECT_REF }}
          SUPABASE_ANON_KEY: ${{ secrets.FINDIT_SUPABASE_ANON_KEY }}
          PUSH_DISPATCH_TOKEN: ${{ secrets.FINDIT_PUSH_DISPATCH_TOKEN }}
          FINDIT_WEB_PUSH_WORKERS_ENABLED: ${{ vars.FINDIT_WEB_PUSH_WORKERS_ENABLED }}
        run: |
          set -euo pipefail
          if [[ "${FINDIT_WEB_PUSH_WORKERS_ENABLED:-false}" != "true" ]]; then
            echo 'Staging Web Push delivery is disabled for this environment.'
            exit 0
          fi
          test -n "$SUPABASE_URL"
          test -n "$EXPECTED_PROJECT_REF"
          test "$SUPABASE_URL" = "https://${EXPECTED_PROJECT_REF}.supabase.co"
          test -n "$SUPABASE_ANON_KEY"
          test -n "$PUSH_DISPATCH_TOKEN"

      - name: Deliver pending staging push notifications
        env:
          SUPABASE_URL: ${{ vars.VITE_SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.FINDIT_SUPABASE_ANON_KEY }}
          PUSH_DISPATCH_TOKEN: ${{ secrets.FINDIT_PUSH_DISPATCH_TOKEN }}
          FINDIT_WEB_PUSH_WORKERS_ENABLED: ${{ vars.FINDIT_WEB_PUSH_WORKERS_ENABLED }}
        run: |
          set -euo pipefail
          if [[ "${FINDIT_WEB_PUSH_WORKERS_ENABLED:-false}" != "true" ]]; then
            echo 'Staging Web Push delivery is disabled for this environment.'
            exit 0
          fi
          curl --fail --silent --show-error \
            --retry 2 --retry-all-errors --max-time 45 \
            -X POST "$SUPABASE_URL/functions/v1/web-push-dispatch" \
            -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            -H "x-push-dispatch-token: $PUSH_DISPATCH_TOKEN" \
            -H "content-type: application/json" \
            --data '{"limit":50}'
