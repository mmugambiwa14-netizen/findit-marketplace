# WP-02 — CI recovery and newly exposed repository defects

## Findings

- F-012 — quality/deployment workflows were unusable as a release proof boundary.
- F-058 — `traceContext.js` bypassed the guarded browser-storage abstraction.
- F-060 — buyer certification ran `npm ci` without normalizing the repository lockfile.
- F-061 — Pages preview used a mutable `actions/checkout@v4` tag.

## Implementation

Commit: `7e5b0842d098f68f695d3d5e4bafb51e083b6877`

Changed:

- `src/lib/traceContext.js`
  - replaced direct `sessionStorage` calls with `readStoredString` and `writeStoredString` from `src/lib/browserStorage.js`;
  - retained the existing random trace fallback and did not weaken blocked-storage behavior.
- `.github/workflows/buyer-journey-certification.yml`
  - added `node ./scripts/normalize-package-lock.mjs --write` before hosted `npm ci`.
- `.github/workflows/pages-preview.yml`
  - replaced mutable `actions/checkout@v4` with immutable SHA `3d3c42e5aac5ba805825da76410c181273ba90b1`.

## Execution evidence

Draft PR: `#33`

### Release candidate gates

- Run: `31146750894`
- Job: `92767637096`
- Contract summary: 769 tests, 762 passed, 7 failed.
- Prior summary: 769 tests, 759 passed, 10 failed.
- Improvement: the three contracts owned by F-058/F-060/F-061 are now green.

The following steps completed successfully:

- repository hygiene;
- source graph;
- SQL rollback boundary;
- deployment security;
- MapLibre asset verification;
- product-surface audit;
- lint;
- application typecheck;
- migration typecheck;
- active V1 typecheck;
- Edge Function typecheck;
- Security Advisor baseline;
- product audit;
- production build;
- internal certification.

The overall job remained red solely because seven contracts owned by later work packages remain intentionally unresolved:

- five F-017 PWA asset contracts — WP-19;
- one F-042 listing-card derivative contract — WP-18;
- one F-029 sold-transition contract — WP-15.

### Buyer Journey Certification

- Run: `31146750853`
- Contracts job: passed.

## Closure decision

WP-02 is `DONE` at its approved repository boundary because:

1. F-058, F-060 and F-061 each have a previously failing contract that now passes in CI.
2. The product-surface audit passes.
3. The early-gate cascade remains repaired: later verification stages executed after the contract failure.
4. Every remaining contract failure has an explicit later finding and work-package owner.

This is **not** a claim that all release gates are green or that staging is deployed.

## Deliberately not done

- No tests were deleted or weakened.
- No security control was bypassed.
- No listing moderation, Peek moderation, payments or reputation system was introduced.
- No product-code workaround was added for the Vercel plan/quota failure.
- Branch protection, Pages configuration and environment separation were not marked verified without external evidence.
