// Keep the route-level page name stable while using the complete moderation
// surface. The old page only exposed approve/reject and used a status
// vocabulary that no longer matched admin_tour_queue_page.
export { default } from '@/components/admin/AdminTourQueue';
